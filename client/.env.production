# Production API base URL. Loaded automatically by `vite build` (which runs in
# production mode by default) and never by `vite dev`, so local development
# keeps using the VITE_API_BASE_URL from .env (http://localhost:4000).
VITE_API_BASE_URL=https://agri-map-api.onrender.com
